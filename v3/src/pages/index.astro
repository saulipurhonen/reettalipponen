---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getContent } from '../lib/content';

interface HomeData {
  title: string;
  description: string;
}

const { data, content } = getContent<HomeData>('fi/home/index.md');
---

<BaseLayout title={data.title} description={data.description}>
  <link
    slot="head"
    rel="preload"
    as="image"
    type="image/webp"
    imagesrcset="/images/home_bg_640.webp 640w, /images/home_bg_1024.webp 1024w, /images/home_bg_1920.webp 1920w"
    imagesizes="100vw"
    fetchpriority="high"
  />

  <!-- Hero: full viewport, dramatic — negative margin pulls behind sticky nav -->
  <section
    aria-label="Esittely"
    class="hero-section relative flex flex-col h-svh lg:min-h-svh lg:h-auto -mt-[76px] overflow-hidden"
  >
    <!-- Background image -->
    <picture>
      <source
        type="image/webp"
        srcset="/images/home_bg_640.webp 640w, /images/home_bg_1024.webp 1024w, /images/home_bg_1920.webp 1920w"
        sizes="100vw"
      />
      <img
        src="/images/home_bg_1920.jpg"
        srcset="/images/home_bg_640.jpg 640w, /images/home_bg_1024.jpg 1024w, /images/home_bg_1920.jpg 1920w"
        sizes="100vw"
        alt="Kuvataiteilija Reetta Lipponen"
        class="hero-img absolute inset-0 w-full h-full object-cover object-[0%_30%]"
        loading="eager"
        fetchpriority="high"
        decoding="async"
      />
    </picture>

    <!-- Layered gradient: richer, more cinematic -->
    <div
      class="absolute inset-0 bg-gradient-to-t from-surface via-surface/60 to-surface/10"
      aria-hidden="true"
    >
    </div>

    <!-- Subtle organic noise texture -->
    <div class="hero-noise absolute inset-0" aria-hidden="true"></div>

    <!-- Hero content -->
    <div
      class="relative z-10 w-full mt-auto px-6 pb-4 sm:pb-16 lg:pb-20 lg:px-12"
    >
      <div
        class="mx-auto max-w-7xl lg:flex lg:items-end lg:justify-between lg:gap-16"
      >
        <!-- Left: name + subtitle -->
        <div class="lg:max-w-2xl">
          <p
            class="hero-subtitle text-sm sm:text-base font-semibold uppercase tracking-[0.25em] text-gold mb-3 sm:mb-6"
          >
            Kuvataiteilija
          </p>
          <h1
            class="hero-title font-display text-5xl sm:text-8xl lg:text-[9rem] lg:leading-[0.85] font-light tracking-tight text-text"
          >
            {data.title}
          </h1>
          <!-- Gold accent line — spans full title width -->
          <div
            class="hero-line mt-4 sm:mt-8 h-px w-full bg-gold/70"
            aria-hidden="true"
          >
          </div>
        </div>

        <!-- Right: intro text on desktop only -->
        <div class="hero-intro hidden lg:block lg:max-w-sm lg:pb-2">
          <div
            class="prose prose-theme prose-lg max-w-none text-text-muted/90 leading-relaxed"
            set:html={content}
          />
        </div>
      </div>
    </div>

    <!-- Scroll indicator — mobile/tablet only (desktop has intro text in hero) -->
    <div
      class="scroll-indicator relative z-10 flex flex-col items-center pb-6 lg:hidden"
      aria-hidden="true"
    >
      <span class="block w-px h-8 bg-gradient-to-b from-transparent to-gold/50"
      ></span>
    </div>
  </section>

  <!-- Intro section: below the fold on mobile only -->
  <section
    aria-label="Sisältö"
    class="reveal mx-auto max-w-2xl px-6 pt-10 pb-20 sm:py-28 lg:hidden"
  >
    <div class="relative">
      <div
        class="absolute -left-4 top-0 w-px h-full bg-gradient-to-b from-gold/40 via-gold/10 to-transparent"
        aria-hidden="true"
      >
      </div>
      <div
        class="prose prose-theme prose-lg max-w-none text-text-muted leading-relaxed"
        set:html={content}
      />
    </div>
  </section>
</BaseLayout>

<style>
  /* Organic noise texture overlay */
  .hero-noise {
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    opacity: 0.03;
    mix-blend-mode: overlay;
    pointer-events: none;
  }

  /* Scroll indicator pulse */
  .scroll-indicator span {
    animation: scroll-pulse 2s ease-in-out infinite;
  }

  @keyframes scroll-pulse {
    0%,
    100% {
      opacity: 0.3;
      transform: scaleY(1);
    }
    50% {
      opacity: 0.8;
      transform: scaleY(1.3);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .scroll-indicator span {
      animation: none;
      opacity: 0.5;
    }
  }
</style>

<script>
  import gsap from 'gsap';

  document.addEventListener('astro:page-load', () => {
    const prefersReducedMotion = window.matchMedia(
      '(prefers-reduced-motion: reduce)',
    ).matches;

    const subtitle = document.querySelector('.hero-subtitle');
    const title = document.querySelector('.hero-title');
    const line = document.querySelector('.hero-line');
    const intro = document.querySelector('.hero-intro');
    const scrollInd = document.querySelector('.scroll-indicator');
    const heroImg = document.querySelector('.hero-img');

    if (prefersReducedMotion) {
      gsap.set([subtitle, title, line, intro, scrollInd], {
        opacity: 1,
        y: 0,
        x: 0,
        scale: 1,
      });
      return;
    }

    // Subtle Ken Burns on the hero image
    if (heroImg) {
      gsap.fromTo(
        heroImg,
        { scale: 1.08 },
        { scale: 1, duration: 8, ease: 'power1.out' },
      );
    }

    // Staggered entrance choreography
    const tl = gsap.timeline({ defaults: { ease: 'power3.out' } });

    tl.fromTo(
      subtitle,
      { opacity: 0, y: 20 },
      { opacity: 1, y: 0, duration: 0.7 },
    )
      .fromTo(
        title,
        { opacity: 0, y: 40 },
        { opacity: 1, y: 0, duration: 0.9 },
        '-=0.4',
      )
      .fromTo(
        line,
        { opacity: 0, scaleX: 0, transformOrigin: 'left center' },
        { opacity: 1, scaleX: 1, duration: 0.6 },
        '-=0.3',
      )
      .fromTo(
        intro,
        { opacity: 0, y: 20 },
        { opacity: 1, y: 0, duration: 0.7 },
        '-=0.2',
      )
      .fromTo(
        scrollInd,
        { opacity: 0 },
        { opacity: 1, duration: 0.5 },
        '-=0.2',
      );
  });
</script>
